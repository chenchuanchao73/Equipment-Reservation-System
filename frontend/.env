# 局域网配置说明：
# 1. 后端服务器应该使用 --host 0.0.0.0 参数启动，以监听所有网络接口
# 2. 前端配置必须使用服务器的实际IP地址
#
# 可以通过以下命令查看服务器IP：
# - Windows: ipconfig
# - Linux/Mac: ifconfig 或 ip addr
#
# 局域网访问配置：
VUE_APP_API_URL=http://10.6.240.41:8000

# 本地开发配置（仅在服务器上访问，取消注释使用）：
# VUE_APP_API_URL=http://localhost:8000
NODE_ENV=development
